活动设置的规则（改动的目的就是为了确保每个开关和每个设置数据的地方都能设置，不再进行跳过）：
如果是开关类的规则，根据配置请求返回的value1的值是"1", 就是开启，否则就是关闭"0"，
针对已开启的状态的开关就先关闭等待1s在开启，如果是关闭的状态就等待1s后开启
如果是设置数字类的设置就是读取value1的值后进行 + 2操作后再设置，
如果是设置字符类的比如字母或者一些特殊字符，一般的处理都是根据我给你提供的payload的数据原封的请求，如果有特殊的要求我会写清楚，
比如后面加上时间戳